apiVersion: nest.crossplane.io/v1
kind: App
metadata:
  name: firstapp
  namespace: apps-lab
spec:
  image: nginx
  # command: ["/bin/sh"]
  # args: ["-c", "echo hello"]
  port: 80
  minReplicas: 1
  maxReplicas: 5
  utilization: 60
  compositionSelector:
    matchLabels:
      ingress: "true"
  host: appslab.test.nawnest.my.id
  envFile: "VEVTVCA9IElMSEFN"